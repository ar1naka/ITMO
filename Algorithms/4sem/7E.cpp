#pragma GCC optimize("Ofast")

#include <vector>
#include <iostream>

using namespace std;

typedef long long ll;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    ll p;
    cin >> p;

    if (p == 2) {
        cout << 1 << endl;
        return 0;
    }

    cout << 0 << endl;

    return 0;
}